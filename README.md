# 電子學互動講義

把電機系電子學（Neamen, *Microelectronics: Circuit Analysis and Design*, 4e）的課堂投影片，
改寫成可以動手操作的網頁：每個觀念都配一個可拉、可點、可玩的互動模組，而不是一張靜態圖。

線上瀏覽：開啟 `index.html` 即可，**不需要任何建置流程**（純 HTML + CSS + Canvas，零相依套件）。

```
index.html          首頁：課程地圖 + CH1 PART 1 全部模組
assets/app.css      設計語彙（色票、字體、版面）
assets/app.js       互動引擎（Canvas 舞台、主題色、繪圖原語）
assets/ch1.js       CH1 PART 1 的 8 個互動模組 + 小測驗
docs/CONTENT_MAP.md 章節分類與上傳進度
```

## 目前已完成：CH1 PART 1（投影片 p.1–30）

| # | 模組 | 對應投影片 | 你可以做什麼 |
|---|------|-----------|-------------|
| 01 | 訊號極性實驗室 | p.3 | 拉 Vdc / Vac，看同一條式子在 AC、DC、脈動 DC 之間切換 |
| 02 | 原子殼層建構器 | p.4, 11–15 | 調原子序，看殼層填法、價電子數與族別 |
| 03 | 化學鍵實驗台 | p.16–19 | 播放 Na→Cl 的電子轉移，或 Si–Si 的電子共用 |
| 04 | 矽晶格熱擾動 | p.18–22 | 拉溫度，看共價鍵斷裂成對產生電子與電洞 |
| 05 | 能帶圖 | p.20–21 | 調 Eg 與 T，看禁止能隙撐開、躍遷變頻繁 |
| 06 | 電洞移動挑戰 | p.24 | 小遊戲：點對該跳的價電子，把電洞推著走 |
| 07 | ni 計算器 | p.25–26 | Si / Ge / GaAs 的 ni–T 對數曲線與即時數值 |
| 08 | N 型摻雜實驗室 | p.27–30 | 點一下把 Si 換成 P；另附 n·p = ni² 的定量面板 |
| 09 | 觀念小測驗 | 全段 | 10 題，答錯會直接解釋為什麼 |

## 設計原則

- **定量數值一律即時計算**，用課本表 1.3 的 B 與 Eg 代入 `ni = B·T^(3/2)·exp(−Eg/2kT)`，
  不寫死答案。
- **示意與真實分開講**：晶格上畫得出來的斷鍵數量是壓縮過的（真實比例下 300 K 每 10¹² 個矽原子
  才有一個載子），畫面會標明，旁邊的數字才是真實量級。
- 明暗主題自動跟隨系統，也可手動切換；手機寬度（~390px）下版面會收成單欄。

## 之後要加新章節

1. 把新的講義 PDF 丟進對話。
2. 在 `assets/` 下新增 `chN.js`，沿用 `assets/app.js` 匯出的 `window.__EE`
   （`Stage`、`electron`、`hole`、`bindRange` 等）撰寫模組。
3. 更新 `docs/CONTENT_MAP.md` 與首頁課程地圖上的狀態。
