# 工程數學 — 單元分類與上傳進度

課程：大二上工程數學。以課堂手寫筆記為準。

## 已完成

### 拉普拉斯轉換 Laplace Transform（9/17 – 9/20 筆記，共 3 頁）

| # | 主題 | English | 互動模組 |
|---|------|---------|----------|
| 01 | 什麼是「轉換」 | What is a transform | 轉換機器動畫（生活例子 / ℒ 雙模式） |
| 02 | 實變數 vs 複變數函數 | real vs complex functions | 雙複數平面，可拖曳 z 看 w = f(z) |
| 03 | 為什麼乘 e⁻ˢᵗ 再積分 | why the kernel e^(−st) | 三層曲線 + 面積 ↔ F(s) 對照，含 ROC 發散警告 |
| 04 | 基本轉換對照表 | transform pairs | 可展開的推導（1, t, e^(at), cos, sin） |
| 05 | 微分性質推導 | derivative property | 9 步驟逐步推導 + t/s 域對應圖 |
| 06 | 為什麼多了 −f(0) | why the −f(0) term | 三個例子四格對照（正確 vs 漏寫） |
| 07 | 用 ℒ 解 ODE | solving ODEs | 三步驟流程圖，兩題可切換 |

另含：**筆記勘誤**（2 處錯誤 + 1 處待確認）、名詞中英對照表（17 詞）、12 題中英對照測驗。

## 本單元核心公式

```
定義          ℒ(f(t)) = F(s) = ∫₀^∞ f(t) e^(−st) dt
定義域→值域    { f(t) | t ∈ ℝ } 實變數函數  →  { F(s) | s = σ+jω ∈ ℂ } 複變數函數
尤拉公式       e^(iθ) = cos θ + i sin θ   ⟹  e^(−iy) = cos y − i sin y   ← 注意減號
微分性質       ℒ(f′)  = sF(s) − f(0)
              ℒ(f″)  = s²F(s) − s f(0) − f′(0)
轉換對         1        ↔ 1/s           (s > 0)
              t        ↔ 1/s²          (s > 0)
              e^(at)   ↔ 1/(s−a)       (s > a)   ← ROC
              cos ωt   ↔ s/(s²+ω²)     (s > 0)
              sin ωt   ↔ ω/(s²+ω²)     (s > 0)
解 ODE 流程    t 域 ODE →(ℒ)→ s 域代數 → 解出 Y(s) →(ℒ⁻¹)→ y(t)
```

## 筆記勘誤紀錄（9/17–9/20）

1. **e⁻ᶻ 的展開符號錯誤**：筆記寫 `e⁻ˣ(cos y + i sin y)`，應為 `e⁻ˣ(cos y − i sin y)`，
   故 `Im = −e⁻ˣ sin y`。驗證：x=0, y=π/2 時 e^(−iπ/2) = −i。
2. **Theorem 下方方框筆誤**：寫成 `sF(s) = f(0)`，應為 `sF(s) − f(0)`。
   （上一行 Theorem 本身是對的，判斷為手寫筆誤。）
3. **待確認**：`∫t²dt = ⅓t?+C` 手寫不清，正確為 `⅓t³+C`。
   （其下 `∫₁² t²dt = 7/3` 正確。）

## 待上傳

拉普拉斯轉換後續（反轉換、部分分式、平移定理、摺積、解高階 ODE）
以及其他單元（複變、線代、傅立葉、偏微分方程…）尚未上傳。
